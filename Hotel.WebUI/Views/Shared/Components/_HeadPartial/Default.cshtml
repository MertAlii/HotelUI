<head>
    <meta charset="utf-8">
    <title>Alkan Hotel - Lüks Konaklama ve Eşsiz Tatil Deneyimi</title>
    <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes" name="viewport">
    <meta content="otel, konaklama, tatil, rezervasyon, lüks otel, Alkan Hotel" name="keywords">
    <meta content="Alkan Hotel'de eşsiz konaklama deneyimi yaşayın. Modern odalar, birinci sınıf hizmet ve unutulmaz anılar için hemen rezervasyon yapın." name="description">

    <!-- Favicon -->
    <link href="~/hotel-html-template/img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="~/hotel-html-template/lib/animate/animate.min.css" rel="stylesheet">
    <link href="~/hotel-html-template/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">
    <link href="~/hotel-html-template/lib/tempusdominus/css/tempusdominus-bootstrap-4.min.css" rel="stylesheet" />

    <!-- Customized Bootstrap Stylesheet -->
    <link href="~/hotel-html-template/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="~/hotel-html-template/css/style.css" rel="stylesheet">
    
    <!-- Modern Responsive CSS -->
    <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary: #f59e0b;
            --light: #f8fafc;
            --dark: #1e293b;
            --gray-100: #f1f5f9;
            --gray-200: #e2e8f0;
            --gray-300: #cbd5e1;
            --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }

        /* Modern Typography */
        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.7;
            font-size: 16px;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
            font-weight: 600;
            line-height: 1.3;
        }

        /* Responsive Typography - büyük ekranlar için */
        media (min-width: 1400px) {
            body { font-size: 18px; }
            h1 { font-size: 3.5rem; }
            h2 { font-size: 2.8rem; }
            h3 { font-size: 2.2rem; }
            .display-3 { font-size: 4.5rem; }
            .display-4 { font-size: 3.5rem; }
            .lead { font-size: 1.4rem; }
        }

        media (min-width: 1920px) {
            body { font-size: 20px; }
            h1 { font-size: 4rem; }
            h2 { font-size: 3.2rem; }
            h3 { font-size: 2.6rem; }
            .display-3 { font-size: 5rem; }
            .display-4 { font-size: 4rem; }
            .lead { font-size: 1.5rem; }
        }

        /* Orta ekranlar için */
        media (min-width: 992px) and (max-width: 1399px) {
            .display-3 { font-size: 3.5rem; }
            .display-4 { font-size: 2.8rem; }
        }

        /* Küçük ekranlar için - mobil */
        media (max-width: 991px) {
            h1 { font-size: 1.8rem !important; }
            h2 { font-size: 1.6rem !important; }
            h3 { font-size: 1.4rem !important; }
            .display-3 { font-size: 2.2rem !important; }
            .display-4 { font-size: 1.8rem !important; }
            .lead { font-size: 1.1rem !important; }
        }

        /* Modern Button Styles */
        .btn {
            border-radius: 12px;
            font-weight: 500;
            padding: 12px 24px;
            transition: all 0.3s ease;
            border: none;
            position: relative;
            overflow: hidden;
            text-transform: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            box-shadow: var(--shadow);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
            background: linear-gradient(135deg, var(--primary-dark), var(--primary));
        }

        .btn-lg {
            padding: 16px 32px;
            font-size: 1.1rem;
        }

        /* Responsive buttons */
        media (max-width: 768px) {
            .btn {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
            
            .btn-lg {
                padding: 12px 24px;
                font-size: 1rem;
            }
        }

        /* Modern Card Styles */
        .modern-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            border: 1px solid var(--gray-200);
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .modern-card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-lg);
        }

        /* Glass Effect */
        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        /* Section Spacing */
        section {
            padding: 80px 0;
        }

        media (max-width: 768px) {
            section {
                padding: 60px 0;
            }
        }

        media (min-width: 1400px) {
            section {
                padding: 120px 0;
            }
        }
    </style>
</head>